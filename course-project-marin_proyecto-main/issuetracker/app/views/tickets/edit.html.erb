<br>
<%= form_with(model: @ticket) do |form| %>
  <% if @ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>
      <ul>
        <% @ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
    <%else%>
    <h3><%=@ticket.title%></h3>
        <h6><strong>Ticket id: </strong><%=@ticket.id%></h6>
        <h5><strong>User: </strong><%=@ticket.user.first_name%><%=" "%><%=@ticket.user.last_name%></h5>        
        <ul class="list-unstyled">
        <h5><strong>Description: </strong><%=@ticket.description%></h5>
    <div class="col">
      <%= form.label :priority, class: "form-label" %>
    <%= form.select :priority,[["Urgent", "High"], ["Normal", "Half"], ["Low", "Low"]], selected: :priority%>
    </div>
    <div class="col">
      <%= form.label :state, class: "form-label" %>
    <%= form.select :state, [["open", "open"], ["close", "close"], ["reopen", "reopen"]], selected: :state %>
    </div>
    <div class="mb-3">
        <%= form.label :category, class: "form-label" %>
        <%= form.text_field :category, class: "form-control" %>
      </div>
    <div class="mb-3">
        <%= form.label :tags, class: "form-label" %>
        <%= form.text_area :tags, class: "form-control" %>
      </div>
       <div class="mb-3">
        <%= form.label :key_resolution, class: "form-label" %>
        <%= form.text_area :key_resolution, class: "form-control" %>
      </div>
      
 <div>
  <%= form.submit %>
  </div> 

  <% end %>  
<% end %>

<div>
  <%= link_to "Back to tickets", tickets_path %>
</div>
