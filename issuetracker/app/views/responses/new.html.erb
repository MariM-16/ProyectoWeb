<h1>New response</h1>
 
<%= form_with(model: @response) do |form| %>
  <% if @response.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
        <% @response.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
    <%else%>
    <form>
<h4><strong><%="Ticket title"%></strong><%=" "%><%=@ticket.title%></h4> 
 <h6><strong><%="Ticket ID"%></strong><%=" "%><%=@ticket.id%></h6>
 <h5><strong><%="User"%></strong><%=" "%><%=@ticket.user.first_name%><%=" "%><%=@ticket.user.last_name%></h5>
  <%=form.hidden_field :user_id, value: @ticket.executive.id%>
  <%=form.hidden_field :ticket_id, value: @ticket.id%>
  <%=form.hidden_field :response_date, value: Time.now%>

  <div class="form-group">
  <h5><strong> Response </strong></h5>
  <div id="colp"><%= form.text_area :response, class: "form-control"%></div>
  </div>
  <div>
    <%= form.submit %>
  </div>
  </form>
  <% end %>
<% end %>
<br>

<div>
  <%= link_to "Back to responses", responses_path %>
</div>
